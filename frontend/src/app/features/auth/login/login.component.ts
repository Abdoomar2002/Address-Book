import { Component } from '@angular/core';

// PLACEHOLDER - replaced by the real login form.
@Component({
  selector: 'app-login',
  standalone: true,
  template: `<h2>Login</h2>`
})
export class LoginComponent {}
